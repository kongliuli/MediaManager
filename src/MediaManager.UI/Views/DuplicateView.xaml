<UserControl x:Class="MediaManager.UI.Views.DuplicateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:conv="clr-namespace:MediaManager.UI.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <conv:FileSizeConverter x:Key="FileSize"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 + 操作 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="重复文件" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"/>
            <Button HorizontalAlignment="Right" Style="{StaticResource ButtonPrimary}"
                    Content="扫描重复文件" Command="{Binding ScanDuplicatesCommand}"/>
        </Grid>

        <!-- 统计 -->
        <hc:Card Grid.Row="1" Margin="0,0,0,16"
                 Visibility="{Binding Groups.Count, Converter={StaticResource BoolToVis}}">
            <StackPanel Orientation="Horizontal" Margin="16,12">
                <TextBlock Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13">
                    <Run Text="发现 "/>
                    <Run Text="{Binding Groups.Count, Mode=OneWay}" FontWeight="SemiBold"
                         Foreground="{DynamicResource PrimaryBrush}"/>
                    <Run Text=" 组重复文件，可释放 "/>
                    <Run Text="{Binding TotalReclaimable, Converter={StaticResource FileSize}}"
                         FontWeight="SemiBold" Foreground="{DynamicResource DangerBrush}"/>
                </TextBlock>
            </StackPanel>
        </hc:Card>

        <!-- 重复组列表 -->
        <hc:LoadingCircle Grid.Row="2" IsRunning="{Binding IsLoading}"
                          Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Groups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <hc:Card Margin="0,0,0,12">
                            <StackPanel Margin="16">
                                <TextBlock FontSize="12" Margin="0,0,0,8"
                                           Foreground="{DynamicResource SecondaryTextBrush}">
                                    <Run Text="{Binding Files.Count, Mode=OneWay}"/>
                                    <Run Text=" 个重复文件 · 可释放 "/>
                                    <Run Text="{Binding ReclaimableSize, Mode=OneWay, Converter={StaticResource FileSize}}"
                                         Foreground="{DynamicResource DangerBrush}"/>
                                </TextBlock>
                                <ItemsControl ItemsSource="{Binding Files}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Path}"
                                                           FontSize="12" TextTrimming="CharacterEllipsis"
                                                           Foreground="{DynamicResource PrimaryTextBrush}"
                                                           ToolTip="{Binding Path}"/>
                                                <TextBlock Grid.Column="1" Margin="12,0"
                                                           Text="{Binding FileSize, Converter={StaticResource FileSize}}"
                                                           FontSize="12"
                                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                <Button Grid.Column="2"
                                                        Style="{StaticResource ButtonDanger}"
                                                        Content="删除" Padding="8,2" FontSize="11"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </hc:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

# MediaManager 项目架构报告

## 1. 项目概述

MediaManager 是一个媒体文件管理系统，主要用于管理和组织音频和视频文件。系统提供了媒体文件扫描、元数据提取、重复文件检测、播放列表管理和标签管理等功能。

### 核心功能
- 媒体文件扫描和索引
- 元数据自动提取
- 重复文件检测
- 播放列表管理
- 标签管理
- 缩略图和波形图生成
- 媒体文件搜索

## 2. 架构设计

项目采用了典型的分层架构，遵循依赖倒置原则，各层之间通过接口进行通信。

### 架构层次

| 层次 | 项目 | 职责 |
|------|------|------|
| 核心层 | MediaManager.Core | 定义领域模型、接口和共享类型 |
| 数据层 | MediaManager.Data | 数据访问和持久化 |
| 服务层 | MediaManager.Services | 业务逻辑和服务实现 |
| UI层 | MediaManager.UI | 用户界面和交互 |

### 依赖关系
- UI 层依赖于 Services 层和 Core 层
- Services 层依赖于 Data 层和 Core 层
- Data 层依赖于 Core 层
- Core 层不依赖于其他层

## 3. 核心组件

### 3.1 领域模型

系统采用了继承结构来表示不同类型的媒体文件：

- **MediaFile**：抽象基类，包含所有媒体文件共有的属性
- **AudioFile**：音频文件，继承自 MediaFile，包含音频特有的属性
- **VideoFile**：视频文件，继承自 MediaFile，包含视频特有的属性

### 3.2 数据访问

- **MediaDbContext**：EF Core 数据库上下文，使用 TPH (Table Per Hierarchy) 策略存储不同类型的媒体文件
- **仓储接口**：定义了数据访问的抽象
- **仓储实现**：具体的数据访问实现

### 3.3 服务组件

- **文件扫描服务**：递归遍历目录，发现支持的媒体文件
- **元数据服务**：使用 FFprobe 提取媒体文件的元数据
- **哈希服务**：计算文件哈希值，用于重复检测
- **缩略图服务**：生成视频文件的缩略图
- **波形图服务**：生成音频文件的波形图
- **重复检测服务**：检测重复的媒体文件
- **搜索服务**：搜索媒体文件
- **播放服务**：媒体文件播放功能

### 3.4 UI 组件

- **MainWindow**：主窗口
- **App**：应用程序入口，负责依赖注入配置

## 4. 数据模型

### 4.1 核心实体

| 实体 | 描述 | 主要属性 |
|------|------|----------|
| MediaFile | 媒体文件基类 | Id, Path, FileName, FileSize, DurationSeconds, Hash, DateAdded, LastModified, MediaType, Rating, Notes |
| AudioFile | 音频文件 | 继承 MediaFile，增加 BitRate, SampleRate, Channels, Codec, Title, Artist, Album, Year, Genre, TrackNumber, AlbumArtPath, WaveformImagePath |
| VideoFile | 视频文件 | 继承 MediaFile，增加 Width, Height, FrameRate, VideoCodec, AudioCodec, BitRate, Title, ThumbnailPath |
| Tag | 标签 | Id, Name |
| MediaTag | 媒体文件与标签的关联 | MediaFileId, TagId |
| Playlist | 播放列表 | Id, Name, Description |
| PlaylistItem | 播放列表项 | Id, PlaylistId, MediaFileId, OrderIndex |

### 4.2 数据库设计

- 使用 SQLite 作为存储引擎
- 采用 TPH 策略，将 AudioFile 和 VideoFile 存储在同一个 MediaFiles 表中
- 使用 MediaType 枚举作为鉴别器列
- 数据库文件默认存储在 AppData 目录

## 5. 服务设计

### 5.1 服务接口

| 接口 | 描述 | 主要方法 |
|------|------|----------|
| IFileScannerService | 文件扫描服务 | DiscoverFilesAsync, IsSupportedFormat |
| IMetadataService | 元数据提取服务 | ExtractAsync |
| IHashService | 哈希计算服务 | ComputeHashAsync |
| IThumbnailService | 缩略图生成服务 | GenerateThumbnailAsync |
| IWaveformService | 波形图生成服务 | GenerateWaveformAsync |
| IDuplicateDetectionService | 重复检测服务 | FindDuplicatesAsync |
| ISearchService | 搜索服务 | SearchAsync |
| IPlaybackService | 播放服务 | Play, Pause, Stop |

### 5.2 服务实现

- **FileScannerService**：递归扫描目录，返回支持的媒体文件路径
- **FfprobeMetadataService**：使用 FFprobe 提取媒体文件元数据
- **HashService**：计算文件的 SHA256 哈希值
- **ThumbnailService**：使用 FFmpeg 生成视频缩略图
- **WaveformService**：生成音频波形图
- **DuplicateDetectionService**：基于哈希值检测重复文件
- **SearchService**：基于各种条件搜索媒体文件
- **PlaybackService**：提供媒体播放功能

## 6. 技术栈

| 技术/框架 | 用途 | 版本 |
|-----------|------|------|
| .NET | 开发平台 | .NET 8.0+ |
| WPF | 用户界面 | - |
| Entity Framework Core | ORM | - |
| SQLite | 数据库 | - |
| FFmpeg/FFprobe | 媒体处理 | - |
| Serilog | 日志记录 | - |
| Dependency Injection | 依赖注入 | 内置 |

## 7. 依赖注入

系统使用 .NET 内置的依赖注入容器，在 App.xaml.cs 中配置：

- **AddDataServices**：注册数据服务和仓储
- **AddMediaServices**：注册媒体相关服务
- **AddUIServices**：注册 UI 相关服务和 ViewModel

## 8. 潜在问题和改进建议

### 8.1 潜在问题

1. **错误处理**：部分服务缺少详细的错误处理和异常管理
2. **性能优化**：对于大型媒体库，扫描和索引可能会很慢
3. **扩展性**：某些服务的实现可能难以扩展
4. **测试覆盖**：缺乏单元测试和集成测试
5. **UI 响应性**：长时间运行的操作可能会导致 UI 冻结

### 8.2 改进建议

1. **错误处理**：实现统一的错误处理机制，提供详细的错误信息
2. **性能优化**：
   - 实现增量扫描，只扫描变化的文件
   - 使用并行处理提高扫描和索引速度
   - 实现缓存机制，减少重复计算
3. **扩展性**：
   - 采用插件架构，支持自定义元数据提取器
   - 提供 API 接口，支持第三方集成
4. **测试**：增加单元测试和集成测试，提高代码质量
5. **UI 响应性**：
   - 使用异步操作和任务并行库
   - 实现进度反馈机制
6. **安全性**：
   - 实现文件路径验证，防止路径遍历攻击
   - 加强异常处理，避免敏感信息泄露

## 9. 架构评估

### 9.1 优点

1. **清晰的分层架构**：各层职责明确，边界清晰
2. **依赖倒置**：通过接口实现依赖倒置，提高代码可测试性
3. **可扩展性**：模块化设计，易于添加新功能
4. **类型安全**：使用强类型模型，减少运行时错误
5. **现代化技术栈**：使用最新的 .NET 技术和工具

### 9.2 缺点

1. **缺少文档**：代码注释和文档不够完善
2. **测试覆盖不足**：缺乏单元测试和集成测试
3. **性能优化空间**：对于大型媒体库，性能可能不够理想
4. **UI 设计**：UI 组件和交互设计相对简单

## 10. 结论

MediaManager 是一个设计合理的媒体管理系统，采用了现代的分层架构和技术栈。系统具有良好的扩展性和可维护性，能够满足基本的媒体管理需求。通过实施建议的改进措施，可以进一步提高系统的性能、可靠性和用户体验。

---

**报告生成时间**：2026-04-17
**报告作者**：自动生成
# MediaManager 性能优化实现文档

## 📋 概述

本文档详细描述了 MediaManager 项目中实现的性能优化功能，包括：

1. **媒体增强服务** - 视频缩略图、音频波形、图片预览等
2. **缓存服务** - 内存缓存实现和集成
3. **前端性能优化组件** - 虚拟滚动、图片懒加载、性能网格

---

## 🎬 1. 媒体增强服务

### 1.1 服务接口

**文件：** `src/MediaManager.Core/Interfaces/Services/IMediaEnhancementService.cs`

```csharp
public interface IMediaEnhancementService
{
    Task<byte[]?> GenerateVideoThumbnailAsync(string filePath, TimeSpan? timestamp = null);
    Task<byte[]?> GenerateAudioWaveformAsync(string filePath, int width = 400, int height = 100);
    Task<byte[]?> GenerateImagePreviewAsync(string filePath, int maxWidth = 800, int maxHeight = 800);
    Task<string> ExtractAudioFromVideoAsync(string videoPath, string outputPath);
    Task<string> TranscodeVideoAsync(string inputPath, string outputPath, string codec = "h264", int quality = 80);
    Task<byte[]?> GenerateBlurHashAsync(string filePath, int componentsX = 4, int componentsY = 3);
}
```

### 1.2 核心功能

| 功能 | 方法 | 说明 |
|------|------|------|
| 视频缩略图 | `GenerateVideoThumbnailAsync` | 从视频中提取指定时间点的缩略图 |
| 音频波形 | `GenerateAudioWaveformAsync` | 生成音频波形图 PNG |
| 图片预览 | `GenerateImagePreviewAsync` | 生成缩放后的图片预览 |
| BlurHash | `GenerateBlurHashAsync` | 生成图片的 BlurHash 编码 |

### 1.3 实现类

**文件：** `src/MediaManager.Services/Media/MediaEnhancementService.cs`

**技术栈：**
- **视频处理：** 使用 `IThumbnailService` (FFmpeg)
- **音频处理：** 使用 `IWaveformService` (NAudio)
- **图片处理：** 使用 ImageSharp
- **BlurHash：** 内置实现

---

## 🚀 2. 缓存服务

### 2.1 服务接口

**文件：** `src/MediaManager.Core/Interfaces/Services/ICacheService.cs`

```csharp
public interface ICacheService
{
    Task<T?> GetAsync<T>(string key);
    Task SetAsync<T>(string key, T value, TimeSpan? expiration = null);
    Task<bool> TryGetValueAsync<T>(string key, out T? value);
    Task RemoveAsync(string key);
    Task<bool> ExistsAsync(string key);
    Task ClearAsync();
    Task<long> GetMemoryUsage();
    Task<int> GetItemCount();
}
```

### 2.2 实现类

**文件：** `src/MediaManager.Services/Library/CacheService.cs`

**核心特性：**

| 特性 | 说明 |
|------|------|
| 内存限制 | 最大 256MB 内存使用 |
| 自动清理 | LRU 策略自动清理过期/最旧项 |
| 大小估算 | 自动估算缓存项大小 |
| 线程安全 | 使用 ConcurrentDictionary |
| 过期策略 | 支持绝对过期和滑动过期 |

### 2.3 缓存策略

```csharp
// 默认缓存时长
private readonly TimeSpan _cacheDuration = TimeSpan.FromMinutes(30);

// 内存限制
private readonly long _maxMemoryUsage = 256 * 1024 * 1024; // 256MB
```

---

## 🎨 3. 前端性能优化组件

### 3.1 虚拟滚动组件

**文件：** `src/MediaManager.Api/Components/Shared/VirtualScroll.razor`

**使用方式：**

```razor
<VirtualScroll Items="mediaItems" ItemHeight="100" Height="500px">
    <ItemTemplate>
        <div class="media-item">@context.FileName</div>
    </ItemTemplate>
</VirtualScroll>
```

**参数说明：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `Items` | `IList<TItem>` | 数据源 |
| `ItemHeight` | `int` | 每个项的高度（像素） |
| `Height` | `string` | 容器高度 |
| `OverscanCount` | `int` | 预加载数量（默认 2） |
| `ItemTemplate` | `RenderFragment<TItem>` | 项模板 |

**性能优势：**
- 只渲染可视区域内的项
- 减少 DOM 节点数量
- 提升大数据列表的滚动性能

### 3.2 图片懒加载组件

**文件：** `src/MediaManager.Api/Components/Shared/LazyImage.razor`

**使用方式：**

```razor
<LazyImage 
    Src="image.jpg" 
    Alt="描述" 
    BlurHash="LKO2U~V%2Tw=w]~RBVZRi};RPxuw"
    Width="200" 
    Height="200" />
```

**核心技术：**
- 使用 IntersectionObserver API
- 支持 BlurHash 占位图
- 图片进入视口后才开始加载

### 3.3 高性能媒体网格

**文件：** `src/MediaManager.Api/Components/Shared/PerformanceMediaGrid.razor`

**使用方式：**

```razor
<PerformanceMediaGrid 
    Items="mediaItems"
    PageSize="20"
    GetThumbnailUrl="item => item.ThumbnailUrl"
    GetItemName="item => item.FileName"
    GetItemMeta="item => $"{item.Size} · {item.DateAdded}"
    OnItemClick="OnItemClicked"
    IsLoading="isLoading" />
```

**功能特性：**

| 特性 | 说明 |
|------|------|
| 骨架屏 | 加载时显示骨架动画 |
| 懒加载 | 图片使用 `loading="lazy"` |
| 分页加载 | 支持"加载更多"分页 |
| 虚拟滚动就绪 | 可与虚拟滚动配合使用 |

### 3.4 CSS 性能优化

**文件：** `src/MediaManager.Api/wwwroot/css/custom-design.css`

**性能相关样式：**

```css
/* 骨架屏动画 */
.mm-grid-skeleton-item {
    background: linear-gradient(90deg, ...);
    animation: skeleton-loading 1.5s infinite;
}

/* GPU 加速 */
.mm-virtual-scroll {
    will-change: transform;
    contain: layout style paint;
}

/* 懒加载过渡 */
.mm-lazy-image {
    opacity: 0;
    transition: opacity 0.3s ease-out;
}
```

---

## 🔗 4. 缓存集成

### 4.1 缓存媒体增强服务

**文件：** `src/MediaManager.Services/Media/CachedMediaEnhancementService.cs`

**设计模式：** 装饰器模式

```csharp
public class CachedMediaEnhancementService : IMediaEnhancementService
{
    private readonly IMediaEnhancementService _innerService;
    private readonly ICacheService _cacheService;
    private readonly TimeSpan _cacheDuration = TimeSpan.FromMinutes(30);
    
    // 自动缓存所有增强服务的输出
}
```

### 4.2 服务注册

**文件：** `src/MediaManager.Services/Extensions/ServicesExtensions.cs`

```csharp
services.AddMemoryCache();

// 媒体增强（带缓存）
services.AddSingleton<MediaEnhancementService>();
services.AddSingleton<IMediaEnhancementService>(sp => 
    new CachedMediaEnhancementService(
        sp.GetRequiredService<MediaEnhancementService>(),
        sp.GetRequiredService<ICacheService>()
    )
);

// 缓存服务
services.AddSingleton<ICacheService, CacheService>();
```

---

## ⚡ 性能优化效果

### 预期性能提升

| 场景 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 首次加载 1000 项列表 | 1000+ DOM 节点 | ~20 个 DOM 节点 | ~50x |
| 重复获取缩略图 | 每次重新生成 | 缓存命中 | ~100x |
| 页面图片加载 | 全部立即加载 | 视口内加载 | 减少 50%+ 请求 |
| 内存使用 | 无限制 | 256MB 限制 | 可控 |

### 缓存命中率预期

| 资源类型 | 预期命中率 |
|----------|------------|
| 缩略图 | 80-90% |
| 波形图 | 70-80% |
| 图片预览 | 60-70% |
| BlurHash | 90-95% |

---

## 📊 监控指标

### 缓存监控 API

```csharp
// 获取缓存统计
var memoryUsage = await _cacheService.GetMemoryUsage(); // 字节数
var itemCount = await _cacheService.GetItemCount();     // 项数量
```

### 性能监控建议

1. **缓存命中率**：监控缓存命中/未命中比例
2. **内存使用**：定期检查内存使用情况
3. **缓存大小**：监控缓存项数量和大小分布
4. **加载时间**：监控页面加载时间和交互响应时间

---

## 🔧 配置与调优

### 缓存配置

```csharp
// 调整内存限制（在 CacheService 中）
private readonly long _maxMemoryUsage = 256 * 1024 * 1024; // 256MB

// 调整缓存时长（在 CachedMediaEnhancementService 中）
private readonly TimeSpan _cacheDuration = TimeSpan.FromMinutes(30);
```

### 前端配置

```razor
<!-- 调整虚拟滚动参数 -->
<VirtualScroll 
    ItemHeight="100"     <!-- 根据实际项高度调整 -->
    Height="500px"      <!-- 根据容器高度调整 -->
    OverscanCount="3"   <!-- 预加载数量 -->
/>

<!-- 调整网格分页大小 -->
<PerformanceMediaGrid PageSize="30" />
```

---

## 🚀 使用建议

### 最佳实践

1. **大数据列表**：使用 `VirtualScroll` 组件
2. **图片密集页面**：使用 `LazyImage` 组件
3. **媒体网格**：使用 `PerformanceMediaGrid` 组件
4. **重复请求**：利用缓存服务自动缓存

### 注意事项

1. **内存限制**：缓存有 256MB 限制，大文件不会被缓存
2. **缓存失效**：文件更新时需要手动清理相关缓存
3. **浏览器兼容性**：懒加载使用 IntersectionObserver，旧浏览器需要 polyfill

---

## 📝 总结

本次实现的性能优化功能包括：

✅ **媒体增强服务**：视频缩略图、音频波形、图片预览、BlurHash
✅ **缓存服务**：内存缓存、LRU 清理、大小限制
✅ **前端性能组件**：虚拟滚动、懒加载、高性能网格
✅ **缓存集成**：自动缓存媒体处理结果

这些优化将显著提升 MediaManager 的用户体验，特别是在处理大量媒体文件时。

---

**文档版本**：1.0.0  
**最后更新**：2026-05-06  
**维护者**：MediaManager 开发团队
